# You should change this, used in docker containers names and Laravel also

# dont use spaces

APP_NAME=orchestrator

# The folder name of this project, usually the same as APP_NAME

# used to mount volumes inside containers

DOCKER_PROJECT_DIR_NAME=orchestrator

# The standard APP_URL of Laravel

APP_URL=http://0.0.0.0:8099

# Database credentials

# please, please, please: use strong password on production env

# there will be a new postgres+postigis container with these credentials

DB_DATABASE=orchestrator
DB_USERNAME=orchestrator
DB_PASSWORD=orchestrator

# Local or prod environment?

## LOCAL

APP_ENV=local
APP_DEBUG=true
LOG_LEVEL=debug
DOCKER_SERVE_PORT=8099

## PROD

#APP_ENV=production
#APP_DEBUG=false
#LOG_LEVEL=warning

## Remember to remove DOCKER_SERVE_PORT row in docker-compose.yml

# The hoqu domain necessary for the HoquClient service

HOQU_URL=http://0.0.0.0:8000
#HOQU_URL=https://hoqu.dev.maphub.it
#HOQU_URL=https://hoqu.maphub.it

#

# Other docker configurations

#

# The (external) phpfpm port, used in docker-compose.yml

DOCKER_PHP_PORT=9199

# The (external) psql port, used in docker-compose.yml

DOCKER_PSQL_PORT=5599

#

# Default Laravel env staff

#

APP_KEY=

DB_CONNECTION=pgsql

# in local set DB_HOST=db

DB_HOST=db
DB_PORT=5432

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DRIVER=local
QUEUE_CONNECTION=database
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS=noreply@webmapp.it
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1

MIX_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
MIX_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

SCRUM_MEET_CODE=rst-ukpg-hfz

# Scheduled Tasks Configuration
# Set to true to enable each scheduled task
ENABLE_STORY_PROGRESS_TO_TODO=false
ENABLE_STORY_SCRUM_TO_DONE=false
ENABLE_SYNC_STORIES_CALENDAR=false
ENABLE_STORY_AUTO_UPDATE_STATUS=false
ENABLE_PROCESS_INBOUND_EMAILS=false

# PDF Footer Configuration
# Customize the footer text displayed in PDF documents generated for documentation
# Use <br> for line breaks in the footer text
PDF_FOOTER="MONTAGNA SERVIZI S.C.P.A.<br>Via Errico Petrella 19 - 20124 - Milano (MI)<br>PEC montagnaserviziscpa@legalmail.it - email info@montagnaservizi.com<br>C.F./P.IVA: 11790660960"

# PDF Logo Configuration
# Path to the logo image file used in PDF documents header
# The logo should be placed in a directory outside the repository (e.g., storage/app/pdf-logo/)
# Default: storage/app/pdf-logo/logo.png
# Supported formats: PNG, JPG, JPEG
#PDF_LOGO_PATH="/path/to/your/logo.png"
