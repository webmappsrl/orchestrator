# DB Sync & Backup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Creare il comando `db:sync` che scarica il dump di produzione da S3 e lo ripristina nel DB locale, e configurare lo scheduler per backup automatico in produzione e sync automatico in tutti gli altri ambienti.

**Architecture:** Un singolo comando Artisan `db:sync` con guard per production, che chiama `db:download` e poi esegue `psql` via Symfony Process. Lo scheduler in `Kernel.php` usa un if/else su `APP_ENV` per decidere quale comando eseguire ogni notte.

**Tech Stack:** Laravel 10, Artisan Commands, Symfony Process, PostgreSQL (`psql`), AWS S3 (già configurato tramite disk `wmdumps`)

---

## File Map

| Azione | File |
|--------|------|
| Crea | `app/Console/Commands/SyncDatabaseCommand.php` |
| Modifica | `app/Console/Kernel.php` |
| Test | `tests/Feature/SyncDatabaseCommandTest.php` |

---

### Task 1: Comando `db:sync` — struttura base con guard

**Files:**
- Create: `app/Console/Commands/SyncDatabaseCommand.php`
- Test: `tests/Feature/SyncDatabaseCommandTest.php`

- [ ] **Step 1: Scrivi il test che verifica il guard in production**

```php
<?php

namespace Tests\Feature;

use Illuminate\Support\Facades\Artisan;
use Tests\TestCase;

class SyncDatabaseCommandTest extends TestCase
{
    public function test_command_refuses_to_run_in_production(): void
    {
        app()->detectEnvironment(fn() => 'production');

        $exitCode = Artisan::call('db:sync');

        $this->assertEquals(1, $exitCode);
        $this->assertStringContainsString(
            'production',
            Artisan::output()
        );
    }
}
```

- [ ] **Step 2: Esegui il test per verificare che fallisce**

```bash
docker exec php81_orchestrator php artisan test --filter=SyncDatabaseCommandTest::test_command_refuses_to_run_in_production
```
Expected: FAIL — "Command db:sync not found"

- [ ] **Step 3: Crea il comando con il guard**

```php
<?php

namespace App\Console\Commands;

use Illuminate\Console\Command;
use Illuminate\Support\Facades\Log;

class SyncDatabaseCommand extends Command
{
    protected $signature = 'db:sync';

    protected $description = 'Scarica il dump di produzione da AWS S3 e lo ripristina nel database locale.';

    public function handle(): int
    {
        if (app()->environment('production')) {
            $this->error('db:sync non può essere eseguito in produzione.');
            Log::error('db:sync -> refused to run in production environment');
            return 1;
        }

        return 0;
    }
}
```

- [ ] **Step 4: Esegui il test per verificare che passa**

```bash
docker exec php81_orchestrator php artisan test --filter=SyncDatabaseCommandTest::test_command_refuses_to_run_in_production
```
Expected: PASS

---

### Task 2: Step download nel comando

**Files:**
- Modify: `app/Console/Commands/SyncDatabaseCommand.php`
- Modify: `tests/Feature/SyncDatabaseCommandTest.php`

- [ ] **Step 1: Aggiungi il test per il download**

Aggiungi questo metodo in `SyncDatabaseCommandTest`:

```php
public function test_command_calls_db_download_in_non_production(): void
{
    app()->detectEnvironment(fn() => 'local');

    // Mock Artisan per verificare che db:download venga chiamato
    Artisan::shouldReceive('call')
        ->once()
        ->with('db:download')
        ->andReturn(0);

    // Il restore fallirà (psql non disponibile in test), ma il download deve essere chiamato
    Artisan::call('db:sync');
}
```

- [ ] **Step 2: Esegui il test per verificare che fallisce**

```bash
docker exec php81_orchestrator php artisan test --filter=SyncDatabaseCommandTest::test_command_calls_db_download_in_non_production
```
Expected: FAIL

- [ ] **Step 3: Aggiungi il download nel comando**

Sostituisci il metodo `handle()` in `SyncDatabaseCommand.php`:

```php
public function handle(): int
{
    if (app()->environment('production')) {
        $this->error('db:sync non può essere eseguito in produzione.');
        Log::error('db:sync -> refused to run in production environment');
        return 1;
    }

    $this->log('db:sync -> is started');

    $this->log('db:sync -> START download from AWS');
    $downloadResult = \Illuminate\Support\Facades\Artisan::call('db:download');
    if ($downloadResult !== 0) {
        $this->log('db:sync -> download FAILED', 'error');
        return 1;
    }
    $this->log('db:sync -> DONE download from AWS');

    return 0;
}

protected function log(string $message, string $type = 'info'): void
{
    Log::$type($message);
    $this->$type($message);
}
```

- [ ] **Step 4: Esegui i test**

```bash
docker exec php81_orchestrator php artisan test --filter=SyncDatabaseCommandTest
```
Expected: tutti PASS

---

### Task 3: Step restore via psql

**Files:**
- Modify: `app/Console/Commands/SyncDatabaseCommand.php`
- Modify: `tests/Feature/SyncDatabaseCommandTest.php`

- [ ] **Step 1: Aggiungi il test per il restore**

Aggiungi in `SyncDatabaseCommandTest`:

```php
public function test_command_fails_gracefully_if_dump_file_missing(): void
{
    app()->detectEnvironment(fn() => 'local');

    \Illuminate\Support\Facades\Artisan::shouldReceive('call')
        ->with('db:download')
        ->andReturn(0);

    // Assicurati che il file non esista
    $path = storage_path('app/database/last-dump.sql.gz');
    if (file_exists($path)) {
        rename($path, $path . '.bak');
    }

    $exitCode = Artisan::call('db:sync');

    $this->assertEquals(1, $exitCode);

    // Ripristina il file se era stato spostato
    if (file_exists($path . '.bak')) {
        rename($path . '.bak', $path);
    }
}
```

- [ ] **Step 2: Esegui il test per verificare che fallisce**

```bash
docker exec php81_orchestrator php artisan test --filter=SyncDatabaseCommandTest::test_command_fails_gracefully_if_dump_file_missing
```
Expected: FAIL

- [ ] **Step 3: Aggiungi il restore nel comando**

Aggiungi `use Symfony\Component\Process\Process;` in cima al file e aggiungi il restore in `handle()` dopo il download:

```php
use Symfony\Component\Process\Process;
```

```php
// Dopo il log 'DONE download from AWS', prima del return 0:

$dumpPath = storage_path('app/database/last-dump.sql.gz');
if (!file_exists($dumpPath)) {
    $this->log('db:sync -> dump file not found at ' . $dumpPath, 'error');
    return 1;
}

$this->log('db:sync -> START restore database');

$db = config('database.connections.pgsql');

$process = Process::fromShellCommandline(
    "gunzip -c {$dumpPath} | psql -U {$db['username']} -h {$db['host']} -p {$db['port']} -d {$db['database']}"
);
$process->setTimeout(600);
$process->setEnv(['PGPASSWORD' => $db['password']]);
$process->run();

if (!$process->isSuccessful()) {
    $this->log('db:sync -> restore FAILED: ' . $process->getErrorOutput(), 'error');
    return 1;
}

$this->log('db:sync -> DONE restore database');
$this->log('db:sync -> finished');

return 0;
```

- [ ] **Step 4: Esegui tutti i test del comando**

```bash
docker exec php81_orchestrator php artisan test --filter=SyncDatabaseCommandTest
```
Expected: tutti PASS

- [ ] **Step 5: Verifica manuale del comando in locale**

```bash
docker exec php81_orchestrator php artisan db:sync
```
Expected: output con i log step-by-step, exit 0

---

### Task 4: Scheduler

**Files:**
- Modify: `app/Console/Kernel.php`

- [ ] **Step 1: Aggiungi le entry allo scheduler**

In `app/Console/Kernel.php`, dentro il metodo `schedule(Schedule $schedule)`, aggiungi alla fine:

```php
if (app()->environment('production')) {
    $schedule->command('db:upload_db_aws')
        ->timezone('Europe/Rome')
        ->dailyAt('02:00')
        ->before(function () {
            Log::info('db:upload_db_aws scheduled job starting');
        })
        ->after(function () {
            Log::info('db:upload_db_aws scheduled job finished');
        });
} else {
    $schedule->command('db:sync')
        ->timezone('Europe/Rome')
        ->dailyAt('03:00')
        ->before(function () {
            Log::info('db:sync scheduled job starting');
        })
        ->after(function () {
            Log::info('db:sync scheduled job finished');
        });
}
```

- [ ] **Step 2: Verifica che lo scheduler sia configurato correttamente**

```bash
docker exec php81_orchestrator php artisan schedule:list
```
Expected: `db:sync` (o `db:upload_db_aws` se APP_ENV=production) appare nella lista con orario `03:00` (o `02:00`).
